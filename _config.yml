# Permalinks
permalink:        pretty

# Setup
url:              'http://johncoene.github.io'

# Set to '' in case of hosting on `http://<your-github-name>.github.io`
baseurl:          '/blog'

title:            Coenism

# Markdown enabled, however don't use more than one paragraph (enforced by `>`)
description:      >
  Young epicurian's feeble attempt at reviving the values of the Enlightenment

# The tags listed as entries in the sidebar
sidebar_tags:     [politics, economics]

# Fallback image and color if not overwritten for tag or post
image:            /blog/public/img/Spinoza.jpg
color:            '#A85641'

author:
  name:           'Jean-Philippe Babylas Coene'
  twitter:        jdatap
  github:         JohnCoene
  photo:          /blog/public/img/about.jpg

  # Markdown enabled, can use multiple paragraphs (enabled by `|`)
  about:          |
    Jean-Philippe Coene values data and individual liberty and writes in a seemingly desperate attempt to revive the values instigated in the Enlightenment period. 
